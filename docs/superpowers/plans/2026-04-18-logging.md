# 后端日志系统实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 packages/server 添加全链路日志追踪，支持通过请求 ID 追踪请求完整生命周期。

**Architecture:** 使用 AsyncLocalStorage 自动传递 reqId，Logger 类封装日志输出，Express 中间件生成 reqId 并绑定到请求上下文。

**Tech Stack:** Node.js AsyncLocalStorage、Express 中间件、TypeScript

---

## 文件结构

### 新增文件

| 文件 | 责责 |
|------|------|
| `src/logger/id.ts` | 生成短格式 UUID |
| `src/logger/context.ts` | AsyncLocalStorage 上下文管理 |
| `src/logger/logger.ts` | Logger 类，格式化输出 |
| `src/logger/middleware.ts` | Express 请求日志中间件 |
| `src/logger/index.ts` | 导出入口 |
| `tests/logger.test.ts` | Logger 单元测试 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `src/index.ts` | 添加中间件，替换 console.log |
| `src/middleware/auth.ts` | 使用 logger |
| `src/routes/projects.ts` | 使用 logger |
| `src/routes/containers.ts` | 使用 logger |
| `src/routes/builds.ts` | 使用 logger |
| `src/routes/terminal.ts` | 使用 logger |
| `src/websocket/server.ts` | WebSocket 连接添加 reqId |
| `src/terminal/terminal-manager.ts` | 使用 logger |
| `src/build/preview-container.ts` | 使用 logger |

---

## Task 1: 请求 ID 生成模块

**Files:**
- Create: `packages/server/src/logger/id.ts`

- [ ] **Step 1: 创建 id.ts 文件**

```typescript
// packages/server/src/logger/id.ts

/**
 * 生成短格式 UUID (8字符)
 * 用于请求追踪标识
 */
export function generateShortId(): string {
  const bytes = new Uint8Array(4);
  crypto.getRandomValues(bytes);
  return Array.from(bytes)
    .map(b => b.toString(16).padStart(2, '0'))
    .join('');
}
```

---

## Task 2: AsyncLocalStorage 上下文模块

**Files:**
- Create: `packages/server/src/logger/context.ts`

- [ ] **Step 1: 创建 context.ts 文件**

```typescript
// packages/server/src/logger/context.ts

import { AsyncLocalStorage } from 'async_hooks';

export interface LogContext {
  reqId: string;
}

const logContextStorage = new AsyncLocalStorage<LogContext>();

/**
 * 获取当前请求上下文
 */
export function getLogContext(): LogContext | undefined {
  return logContextStorage.getStore();
}

/**
 * 在指定上下文中运行函数
 * 所有在该函数内调用的 logger 方法都会自动获取 reqId
 */
export function runWithLogContext<T>(reqId: string, fn: () => T): T {
  return logContextStorage.run({ reqId }, fn);
}
```

---

## Task 3: Logger 类

**Files:**
- Create: `packages/server/src/logger/logger.ts`
- Create: `packages/server/tests/logger.test.ts`

- [ ] **Step 2: 写 Logger 单元测试**

```typescript
// packages/server/tests/logger.test.ts

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { createLogger, setLogLevel, LogLevel } from '../src/logger/logger.ts';
import { runWithLogContext } from '../src/logger/context.ts';

describe('Logger', () => {
  let consoleSpy: {
    log: ReturnType<typeof vi.spyOn>;
    error: ReturnType<typeof vi.spyOn>;
    warn: ReturnType<typeof vi.spyOn>;
  };

  beforeEach(() => {
    consoleSpy = {
      log: vi.spyOn(console, 'log').mockImplementation(() => {}),
      error: vi.spyOn(console, 'error').mockImplementation(() => {}),
      warn: vi.spyOn(console, 'warn').mockImplementation(() => {}),
    };
    setLogLevel('DEBUG');
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('应正确格式化 DEBUG 级别日志', () => {
    const logger = createLogger('test');
    logger.debug('test message');

    expect(consoleSpy.log).toHaveBeenCalled();
    const output = consoleSpy.log.mock.calls[0][0];
    expect(output).toContain('[DEBUG]');
    expect(output).toContain('[test]');
    expect(output).toContain('test message');
  });

  it('应正确格式化 INFO 级别日志', () => {
    const logger = createLogger('server');
    logger.info('server started');

    expect(consoleSpy.log).toHaveBeenCalled();
    const output = consoleSpy.log.mock.calls[0][0];
    expect(output).toContain('[INFO]');
    expect(output).toContain('[server]');
    expect(output).toContain('server started');
  });

  it('应正确格式化 WARN 级别日志', () => {
    const logger = createLogger('auth');
    logger.warn('token expired');

    expect(consoleSpy.warn).toHaveBeenCalled();
    const output = consoleSpy.warn.mock.calls[0][0];
    expect(output).toContain('[WARN]');
    expect(output).toContain('[auth]');
    expect(output).toContain('token expired');
  });

  it('应正确格式化 ERROR 级别日志', () => {
    const logger = createLogger('docker');
    const error = new Error('container not found');
    logger.error('failed to start', error);

    expect(consoleSpy.error).toHaveBeenCalled();
    const output = consoleSpy.error.mock.calls[0][0];
    expect(output).toContain('[ERROR]');
    expect(output).toContain('[docker]');
    expect(output).toContain('failed to start');
    expect(output).toContain('container not found');
  });

  it('应在上下文中自动获取 reqId', () => {
    const logger = createLogger('routes');
    runWithLogContext('abc123', () => {
      logger.info('processing request');
    });

    expect(consoleSpy.log).toHaveBeenCalled();
    const output = consoleSpy.log.mock.calls[0][0];
    expect(output).toContain('[req:abc123]');
  });

  it('无上下文时应不显示 reqId', () => {
    const logger = createLogger('server');
    logger.info('server started');

    expect(consoleSpy.log).toHaveBeenCalled();
    const output = consoleSpy.log.mock.calls[0][0];
    expect(output).not.toContain('[req:');
  });

  it('应根据日志级别过滤输出', () => {
    setLogLevel('INFO');
    const logger = createLogger('test');

    logger.debug('debug message');
    logger.info('info message');

    expect(consoleSpy.log).toHaveBeenCalledTimes(1);
    expect(consoleSpy.log.mock.calls[0][0]).toContain('info message');
  });

  it('ERROR 级别应始终输出', () => {
    setLogLevel('ERROR');
    const logger = createLogger('test');

    logger.debug('debug');
    logger.info('info');
    logger.warn('warn');
    logger.error('error');

    expect(consoleSpy.error).toHaveBeenCalledTimes(1);
    expect(consoleSpy.error.mock.calls[0][0]).toContain('error');
  });
});
```

- [ ] **Step 3: 运行测试验证失败**

Run: `cd packages/server && npm test tests/logger.test.ts`
Expected: FAIL - Logger 模块不存在

- [ ] **Step 4: 实现 Logger 类**

```typescript
// packages/server/src/logger/logger.ts

import { getLogContext } from './context.js';

export type LogLevel = 'DEBUG' | 'INFO' | 'WARN' | 'ERROR';

const LEVEL_PRIORITY: Record<LogLevel, number> = {
  DEBUG: 0,
  INFO: 1,
  WARN: 2,
  ERROR: 3,
};

let currentLevel: LogLevel = process.env.NODE_ENV === 'production' ? 'INFO' : 'DEBUG';

export function setLogLevel(level: LogLevel): void {
  currentLevel = level;
}

export function getLogLevel(): LogLevel {
  return currentLevel;
}

export class Logger {
  private module: string;

  constructor(module: string) {
    this.module = module;
  }

  debug(message: string, ...args: unknown[]): void {
    this.log('DEBUG', message, args);
  }

  info(message: string, ...args: unknown[]): void {
    this.log('INFO', message, args);
  }

  warn(message: string, ...args: unknown[]): void {
    this.log('WARN', message, args);
  }

  error(message: string, error?: Error | unknown): void {
    const errorStr = error instanceof Error
      ? `${error.message}\n${error.stack}`
      : error !== undefined
        ? String(error)
        : '';
    this.log('ERROR', `${message}${errorStr ? ': ' + errorStr : ''}`);
  }

  private log(level: LogLevel, message: string, args: unknown[] = []): void {
    if (LEVEL_PRIORITY[level] < LEVEL_PRIORITY[currentLevel]) {
      return;
    }

    const timestamp = new Date().toISOString().replace('T', ' ').slice(0, 23);
    const context = getLogContext();
    const reqIdPart = context ? `[req:${context.reqId}] ` : '';
    const formatted = `[${timestamp}] [${level}] ${reqIdPart}[${this.module}] ${message}`;

    const outputArgs = args.length > 0 ? [formatted, ...args] : [formatted];

    switch (level) {
      case 'ERROR':
        console.error(...outputArgs);
        break;
      case 'WARN':
        console.warn(...outputArgs);
        break;
      default:
        console.log(...outputArgs);
    }
  }
}

export function createLogger(module: string): Logger {
  return new Logger(module);
}
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd packages/server && npm test tests/logger.test.ts`
Expected: PASS - 所有测试通过

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/logger/logger.ts packages/server/tests/logger.test.ts
git commit -m "feat: add Logger class with level filtering and context support"
```

---

## Task 4: Express 请求日志中间件

**Files:**
- Create: `packages/server/src/logger/middleware.ts`
- Create: `packages/server/tests/logger-middleware.test.ts`

- [ ] **Step 1: 写中间件单元测试**

```typescript
// packages/server/tests/logger-middleware.test.ts

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import request from 'supertest';
import express from 'express';
import { requestLoggingMiddleware } from '../src/logger/middleware.ts';
import { setLogLevel } from '../src/logger/logger.ts';

describe('请求日志中间件', () => {
  let consoleSpy: ReturnType<typeof vi.spyOn>;

  beforeEach(() => {
    consoleSpy = vi.spyOn(console, 'log').mockImplementation(() => {});
    setLogLevel('DEBUG');
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('应为每个请求生成 reqId 并设置响应头', async () => {
    const app = express();
    app.use(requestLoggingMiddleware);
    app.get('/test', (_req, res) => res.json({ ok: true }));

    const res = await request(app).get('/test');

    expect(res.headers['x-request-id']).toBeDefined();
    expect(res.headers['x-request-id']).toHaveLength(8);
  });

  it('应记录请求开始和结束', async () => {
    const app = express();
    app.use(requestLoggingMiddleware);
    app.get('/test', (_req, res) => res.json({ ok: true }));

    await request(app).get('/test');

    const calls = consoleSpy.mock.calls.map(c => c[0]);
    expect(calls.some(c => c.includes('--> GET /test'))).toBe(true);
    expect(calls.some(c => c.includes('<-- 200'))).toBe(true);
  });

  it('不同请求应有不同 reqId', async () => {
    const app = express();
    app.use(requestLoggingMiddleware);
    app.get('/test', (_req, res) => res.json({ ok: true }));

    const res1 = await request(app).get('/test');
    const res2 = await request(app).get('/test');

    expect(res1.headers['x-request-id']).not.toBe(res2.headers['x-request-id']);
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd packages/server && npm test tests/logger-middleware.test.ts`
Expected: FAIL - middleware 模块不存在

- [ ] **Step 3: 实现中间件**

```typescript
// packages/server/src/logger/middleware.ts

import type { Request, Response, NextFunction } from 'express';
import { runWithLogContext } from './context.js';
import { createLogger } from './logger.js';
import { generateShortId } from './id.js';

const logger = createLogger('http');

/**
 * Express 请求日志中间件
 * 为每个请求生成唯一 reqId，记录请求开始和结束
 */
export function requestLoggingMiddleware(
  req: Request,
  res: Response,
  next: NextFunction
): void {
  const reqId = generateShortId();
  const startTime = Date.now();

  // 设置响应头，方便前端追踪
  res.setHeader('X-Request-Id', reqId);

  // 在上下文中运行后续处理
  runWithLogContext(reqId, () => {
    // 记录请求开始
    logger.debug(`--> ${req.method} ${req.path}`);

    // 记录响应完成
    res.on('finish', () => {
      const duration = Date.now() - startTime;
      logger.debug(`<-- ${res.statusCode} (${duration}ms)`);
    });

    next();
  });
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd packages/server && npm test tests/logger-middleware.test.ts`
Expected: PASS - 所有测试通过

- [ ] **Step 5: 提交**

```bash
git add packages/server/src/logger/middleware.ts packages/server/tests/logger-middleware.test.ts
git commit -m "feat: add request logging middleware with reqId tracking"
```

---

## Task 5: Logger 导出入口

**Files:**
- Create: `packages/server/src/logger/index.ts`

- [ ] **Step 1: 创建 index.ts 导出入口**

```typescript
// packages/server/src/logger/index.ts

export { createLogger, setLogLevel, getLogLevel, Logger, type LogLevel } from './logger.js';
export { getLogContext, runWithLogContext, type LogContext } from './context.js';
export { generateShortId } from './id.js';
export { requestLoggingMiddleware } from './middleware.js';
```

- [ ] **Step 2: 提交**

```bash
git add packages/server/src/logger/index.ts
git commit -m "feat: add logger module export entry"
```

---

## Task 6: 整合中间件到 Express 应用

**Files:**
- Modify: `packages/server/src/index.ts:1-60`

- [ ] **Step 1: 修改 index.ts 添加中间件**

将 `packages/server/src/index.ts` 替换为：

```typescript
// packages/server/src/index.ts
import express from 'express';
import cors from 'cors';
import { createServer } from 'http';
import type Database from 'better-sqlite3';
import { getDb } from './db/connection.js';
import { initSchema } from './db/schema.js';
import { createAuthRouter } from './routes/auth.js';
import { createProjectsRouter } from './routes/projects.js';
import { createContainersRouter } from './routes/containers.js';
import { createGitHubRouter } from './routes/github.js';
import { createGitLabRouter } from './routes/gitlab.js';
import { createReposRouter } from './routes/repos.js';
import { createBuildsRouter } from './routes/builds.js';
import { createWebSocketServer } from './websocket/server.js';
import { requestLoggingMiddleware, createLogger } from './logger/index.js';

const logger = createLogger('server');

export function createApp(db: Database.Database): express.Express {
  const app = express();

  app.use(cors());
  app.use(express.json());
  app.use(requestLoggingMiddleware);

  app.get('/api/health', (_req, res) => {
    res.json({ status: 'ok' });
  });

  app.use('/api/auth', createAuthRouter(db));
  app.use('/api/projects', createProjectsRouter(db));
  app.use('/api/projects', createContainersRouter(db));
  app.use('/api/github', createGitHubRouter(db));
  app.use('/api/gitlab', createGitLabRouter(db));
  app.use('/api/repos', createReposRouter(db));
  app.use('/api/builds', createBuildsRouter(db));

  app.use((_req, res) => {
    res.status(404).json({ error: 'Not found' });
  });

  return app;
}

export function startServer(db: Database.Database, port: number = 3001): void {
  const app = createApp(db);
  const server = createServer(app);

  // 初始化 WebSocket 服务器，传入 db 用于终端 WebSocket 权限检查
  createWebSocketServer(server, db);

  server.listen(port, () => {
    logger.info(`Server running on http://localhost:${port}`);
    logger.info(`WebSocket server ready on ws://localhost:${port}`);
  });
}

// 启动入口
if (process.argv[1] && import.meta.url.endsWith(process.argv[1].replace(/\\/g, '/'))) {
  const db = getDb();
  initSchema(db);
  startServer(db, process.env.PORT ? parseInt(process.env.PORT) : 4000);
}
```

- [ ] **Step 2: 运行现有测试确保无破坏**

Run: `cd packages/server && npm test tests/server.test.ts`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/index.ts
git commit -m "feat: integrate request logging middleware into Express app"
```

---

## Task 7: 替换 auth 中间件日志

**Files:**
- Modify: `packages/server/src/middleware/auth.ts:1-36`

- [ ] **Step 1: 修改 auth.ts 使用 logger**

将 `packages/server/src/middleware/auth.ts` 替换为：

```typescript
// packages/server/src/middleware/auth.ts
import type { Request, Response, NextFunction } from 'express';
import jwt from 'jsonwebtoken';
import { createLogger } from '../logger/index.js';

const logger = createLogger('auth');

const DEFAULT_SECRET = 'code-link-dev-secret';

if (!process.env.JWT_SECRET) {
  logger.warn('Using default JWT_SECRET. Set JWT_SECRET in production!');
}

export const JWT_SECRET = process.env.JWT_SECRET || DEFAULT_SECRET;

export function authMiddleware(req: Request, res: Response, next: NextFunction): void {
  const header = req.headers.authorization;
  if (!header || !header.startsWith('Bearer ')) {
    logger.debug('No auth token provided');
    res.status(401).json({ error: '未提供认证令牌' });
    return;
  }

  const token = header.slice(7);
  if (!token) {
    logger.debug('Empty auth token');
    res.status(401).json({ error: '未提供认证令牌' });
    return;
  }

  try {
    const payload = jwt.verify(token, JWT_SECRET);
    if (typeof payload !== 'object' || payload === null || typeof payload.userId !== 'number') {
      logger.warn('Invalid token payload structure');
      res.status(401).json({ error: '无效的认证令牌' });
      return;
    }
    logger.debug(`Token verified for userId=${payload.userId}`);
    (req as any).userId = payload.userId;
    next();
  } catch (err) {
    logger.warn('Token verification failed', err);
    res.status(401).json({ error: '无效的认证令牌' });
  }
}
```

- [ ] **Step 2: 运行 auth 中间件测试**

Run: `cd packages/server && npm test tests/auth-middleware.test.ts`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/middleware/auth.ts
git commit -m "refactor: replace console calls with logger in auth middleware"
```

---

## Task 8: 替换 projects 路由日志

**Files:**
- Modify: `packages/server/src/routes/projects.ts:62-65`

- [ ] **Step 1: 添加 logger 导入并替换 console.error**

在文件顶部添加导入：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('projects');
```

将第 62-65 行的 console.error 替换为：

```typescript
logger.error('创建项目失败', error);
```

- [ ] **Step 2: 运行测试验证**

Run: `cd packages/server && npm test tests/projects.test.ts`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/routes/projects.ts
git commit -m "refactor: replace console.error with logger in projects route"
```

---

## Task 9: 替换 containers 路由日志

**Files:**
- Modify: `packages/server/src/routes/containers.ts`

- [ ] **Step 1: 添加 logger 导入**

在文件顶部添加：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('containers');
```

- [ ] **Step 2: 替换所有 console.error**

将以下 console.error 替换为 logger.error：

| 行号 | 原代码 | 替换为 |
|------|--------|--------|
| 84 | `console.error('启动容器失败:', error);` | `logger.error('启动容器失败', error);` |
| 130 | `console.error('停止容器失败:', error);` | `logger.error('停止容器失败', error);` |
| 169 | `console.error('获取容器状态失败:', error);` | `logger.error('获取容器状态失败', error);` |
| 212 | `console.error('删除容器失败:', error);` | `logger.error('删除容器失败', error);` |

- [ ] **Step 3: 运行测试验证**

Run: `cd packages/server && npm test tests/containers.test.ts`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/routes/containers.ts
git commit -m "refactor: replace console.error with logger in containers route"
```

---

## Task 10: 替换 builds 路由日志

**Files:**
- Modify: `packages/server/src/routes/builds.ts:37`

- [ ] **Step 1: 添加 logger 导入并替换 console.error**

在文件顶部添加：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('builds');
```

将第 37 行替换为：

```typescript
logger.error('Build failed', error);
```

- [ ] **Step 2: 运行测试验证**

Run: `cd packages/server && npm test tests/builds.test.ts`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/routes/builds.ts
git commit -m "refactor: replace console.error with logger in builds route"
```

---

## Task 11: 替换 terminal 路由日志

**Files:**
- Modify: `packages/server/src/routes/terminal.ts:231`

- [ ] **Step 1: 添加 logger 导入并替换 console.error**

在文件顶部添加：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('terminal');
```

将第 231 行替换为：

```typescript
logger.error('Terminal WebSocket error', error);
```

- [ ] **Step 2: 运行测试验证**

Run: `cd packages/server && npm test tests/terminal-route.test.ts`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/routes/terminal.ts
git commit -m "refactor: replace console.error with logger in terminal route"
```

---

## Task 12: 替换 WebSocket 服务器日志

**Files:**
- Modify: `packages/server/src/websocket/server.ts`

- [ ] **Step 1: 添加 logger 导入**

在文件顶部添加：

```typescript
import { createLogger, runWithLogContext, generateShortId } from '../logger/index.js';

const logger = createLogger('websocket');
```

- [ ] **Step 2: 替换 console.log/error 并添加 reqId**

将 setupHandlers 方法中的日志替换：

```typescript
private setupHandlers(): void {
  this.wss.on('connection', (ws, req) => {
    const url = req.url || '/';
    const urlObj = new URL(url, `http://localhost`);

    // 终端 WebSocket 连接: /terminal?projectId=xxx&userId=xxx
    if (urlObj.pathname === '/terminal') {
      const projectIdStr = urlObj.searchParams.get('projectId');
      const userIdStr = urlObj.searchParams.get('userId');

      if (!projectIdStr || !userIdStr) {
        ws.send(JSON.stringify({ type: 'error', message: '缺少 projectId 或 userId 参数' }));
        ws.close();
        return;
      }

      const projectId = parseInt(projectIdStr, 10);
      const userId = parseInt(userIdStr, 10);

      if (isNaN(projectId) || isNaN(userId)) {
        ws.send(JSON.stringify({ type: 'error', message: '无效的 projectId 或 userId' }));
        ws.close();
        return;
      }

      if (!this.db) {
        ws.send(JSON.stringify({ type: 'error', message: '数据库未初始化' }));
        ws.close();
        return;
      }

      const reqId = generateShortId();
      runWithLogContext(reqId, () => {
        logger.info(`Terminal WebSocket connected: projectId=${projectId}, userId=${userId}`);
        handleTerminalConnection(ws, projectId, userId, this.db);
      });
      return;
    }

    // 实时同步 WebSocket 连接
    const reqId = generateShortId();
    (ws as any)._logContext = { reqId };
    runWithLogContext(reqId, () => {
      logger.info('WebSocket client connected');
    });

    ws.on('message', (data) => {
      const context = (ws as any)._logContext;
      if (context) {
        runWithLogContext(context.reqId, () => {
          this.handleMessage(ws, data.toString());
        });
      } else {
        this.handleMessage(ws, data.toString());
      }
    });

    ws.on('close', () => {
      this.handleDisconnect(ws);
    });

    ws.on('error', (error) => {
      const context = (ws as any)._logContext;
      if (context) {
        runWithLogContext(context.reqId, () => {
          logger.error('WebSocket error', error);
        });
      } else {
        logger.error('WebSocket error', error);
      }
    });
  });
}
```

- [ ] **Step 3: 运行测试验证**

Run: `cd packages/server && npm test tests/websocket-server.test.ts`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/websocket/server.ts
git commit -m "refactor: replace console calls with logger in WebSocket server, add reqId tracking"
```

---

## Task 13: 替换 terminal-manager 日志

**Files:**
- Modify: `packages/server/src/terminal/terminal-manager.ts`

- [ ] **Step 1: 添加 logger 导入**

在文件顶部添加：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('terminal-mgr');
```

- [ ] **Step 2: 替换所有 console 调用**

| 行号 | 原代码 | 替换为 |
|------|--------|--------|
| 76 | `console.error(`Terminal session ${sessionId} stream error:`, error);` | `logger.error(`Terminal session ${sessionId} stream error`, error);` |
| 109 | `console.warn(`Session ${sessionId} not found for input`);` | `logger.warn(`Session ${sessionId} not found for input`);` |
| 118 | `console.error(`Failed to write input to session ${sessionId}:`, error);` | `logger.error(`Failed to write input to session ${sessionId}`, error);` |
| 131 | `console.warn(`Session ${sessionId} not found for resize`);` | `logger.warn(`Session ${sessionId} not found for resize`);` |
| 140 | `console.error(`Failed to resize session ${sessionId}:`, error);` | `logger.error(`Failed to resize session ${sessionId}`, error);` |
| 170 | `console.error(`Error closing session ${sessionId}:`, error);` | `logger.error(`Error closing session ${sessionId}`, error);` |

- [ ] **Step 3: 运行测试验证**

Run: `cd packages/server && npm test tests/terminal-manager.test.ts`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/terminal/terminal-manager.ts
git commit -m "refactor: replace console calls with logger in terminal manager"
```

---

## Task 14: 替换 preview-container 日志

**Files:**
- Modify: `packages/server/src/build/preview-container.ts`

- [ ] **Step 1: 添加 logger 导入**

在文件顶部添加：

```typescript
import { createLogger } from '../logger/index.js';

const logger = createLogger('preview');
```

- [ ] **Step 2: 替换所有 console.error**

| 行号 | 原代码 | 替换为 |
|------|--------|--------|
| 76 | `console.error('Failed to stop container:', error);` | `logger.error('Failed to stop container', error);` |
| 96 | `console.error('Failed to stop container by name:', error);` | `logger.error('Failed to stop container by name', error);` |
| 120 | `console.error('Failed to cleanup container:', error);` | `logger.error('Failed to cleanup container', error);` |

- [ ] **Step 3: 运行测试验证**

Run: `cd packages/server && npm test tests/preview-container.test.ts`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/build/preview-container.ts
git commit -m "refactor: replace console.error with logger in preview container"
```

---

## Task 15: 全量测试验证

**Files:**
- None (验证步骤)

- [ ] **Step 1: 运行全部测试**

Run: `cd packages/server && npm test`
Expected: PASS - 所有测试通过

- [ ] **Step 2: 最终提交（如有遗漏文件）**

```bash
git add -A
git commit -m "feat: complete backend logging system with full request tracing"
```

---

## 实现顺序总结

1. Task 1-5: 创建 Logger 核心模块（TDD）
2. Task 6: 整合中间件到 Express 应用
3. Task 7-14: 替换各模块的 console 调用
4. Task 15: 全量测试验证

每个 Task 都包含独立的测试验证，确保改动不破坏现有功能。